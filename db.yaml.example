# ── PyKYCH 数据库配置模板 ──────────────────────────────────
# 复制此文件为 settings/db.yaml 并填入实际值。
# settings/db.yaml 已在 .gitignore 中，不会被提交到版本控制。

mysql:
  # 服务器地址
  host: kych.net

  # 端口 (MySQL 默认 3306)
  port: 3306

  # 数据库用户
  user: pykych

  # 数据库密码
  password: CHANGEME

  # Markdown 文章数据库名
  markdown_db: pykych_md

  # Wikidot 页面数据库名
  wikidot_db: pykych_wiki

  # 字符集
  charset: utf8mb4

  # 连接池配置
  pool:
    minsize: 2
    maxsize: 10
    pool_recycle: 3600
